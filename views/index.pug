extends layout

block content
  h1= title
  p I love Black.


  button(type="button", class="btn btn-danger" onclick ="doReduce()") Press Me
  br
  div#reduce Reduce the value

  script.  
    var count = 20
    var change = -1
    function doReduce() {
      count += change;
      change--;
       document.getElementById("reduce").innerHTML = "Count " + count + " with change " + change;
     }

  a(href="earphones") Search Results